

type Props = {
  className: any
};

const CardStatusChart = (props: Props) => {

  const { className } = props;

  return (
    <div className={`${className}`}>

    </div>
  )
}

export default CardStatusChart;
